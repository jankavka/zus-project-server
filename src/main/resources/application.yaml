spring:
  datasource:
    url: jdbc:mysql://localhost/zusProjectServer?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
    show-sql: true