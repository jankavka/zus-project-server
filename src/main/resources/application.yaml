spring:
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: validate
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

file:
  upload-dir: ${FILE_UPLOADS_DIR:uploads}

carousel:
  upload-dir: ${CAROUSEL_UPLOADS_DIR:carousel-photos}

  jackson:
    mapper:
      accept-case-insensitive-enums: true

server:
  address: 0.0.0.0
  port: 8080
  forward-headers-strategy: framework

youtube:
  api:
    key: AIzaSyBnoAupUJ6oVyeEgnFKuMzOQ-R4XBxXZDk
    channel-id: UCqFOHLSg-jmCS__QRZgmqXw

google:
  calendar:
    # Raw service-account JSON. On the server this comes from the
    # GOOGLE_CALENDAR_CREDENTIALS_JSON env var (set in the server's .env file next
    # to docker-compose.yml); locally it is left blank and the classpath file
    # service_account_json/rscalendar-credentials2.json is used.
    credentials-json: ${GOOGLE_CALENDAR_CREDENTIALS_JSON:}

debug: false

logging:
  level:
    root: DEBUG
    com.zaxxer.hikari: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} %-5level %logger{36} - %msg%n"

basicdata:
  file-path: ./data/basic-data.json

files:
  file-path: ./data/pdf_files

requiredinformation:
  file-path: ./data/required-information.json

entrance-exam:
  file-path: ./data/entrance-exam.json

title-and-content:
  file-path: ./data/title-and-content.json

app:
  search:
    siteDomain: zusdh.cz

